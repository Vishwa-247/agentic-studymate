# ═══════════════════════════════════════════════════════════════════
# Agentic StudyMate — Backend Environment Variables
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════════════════

# ── Supabase ────────────────────────────────────────────────────────
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_KEY=eyJ...           # Service-role key (NOT the anon key)
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# ── Auth / Security ────────────────────────────────────────────────
JWT_SECRET=change-me-in-production

# ── LLM API Keys ───────────────────────────────────────────────────
GROQ_API_KEY=gsk_...                  # Primary Groq key (llama-3.3-70b)
GEMINI_API_KEY=AI...                  # Google Gemini key (optional)

# Optional: per-category Groq keys (fall back to GROQ_API_KEY)
# GROQ_API_KEY_TECHNICAL=gsk_...
# GROQ_API_KEY_APTITUDE=gsk_...
# GROQ_API_KEY_HR=gsk_...

# ── CORS ────────────────────────────────────────────────────────────
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000,http://localhost:5173
# ALLOWED_ORIGIN_REGEX=^https://.*\.lovable\.app$

# ── Service Hosts (defaults shown) ─────────────────────────────────
# SERVICE_HOST=0.0.0.0
# SERVICE_PORT=8002

# ── Service URLs (used by API gateway, defaults shown) ─────────────
# INTERVIEW_COACH_URL=http://127.0.0.1:8002
# RESUME_ANALYZER_URL=http://127.0.0.1:8003
# PROFILE_SERVICE_URL=http://127.0.0.1:8006
# COURSE_GENERATION_URL=http://127.0.0.1:8008
# EVALUATOR_URL=http://127.0.0.1:8010
# ORCHESTRATOR_URL=http://127.0.0.1:8011
# PROJECT_STUDIO_URL=http://127.0.0.1:8012
# JOB_SEARCH_URL=http://127.0.0.1:8013

# ── Misc ────────────────────────────────────────────────────────────
# DEMO_MODE=false
